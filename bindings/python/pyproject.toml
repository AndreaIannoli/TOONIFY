[build-system]
requires = ["maturin>=1.4"]
build-backend = "maturin"

[project]
name = "toonify"
version = "0.1.0"
description = "Python bindings for the TOON converter"
dependencies = []
requires-python = ">=3.8"
